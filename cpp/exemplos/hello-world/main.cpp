#include <iostream> // Biblioteca padrao de entrada e saida (io = input/output = entrada/saida, stream = fluxo)

// Arquivo normalmente usado para testar se o compilador esta funcionando corretamente

int main(void) // Funcao principal do programa
{
    std::cout << "Hello World in C++!" << std::endl; // Saida de dados

    return 0; // Retorna o valor 0 para o sistema operacional
}